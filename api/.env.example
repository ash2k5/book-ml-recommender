# Optional: comma-separated list of allowed CORS origins for browser calls.
# Unset or empty allows all origins (*). In production set this to the web app
# origin, e.g. https://tome.ask2k5.com
ALLOWED_ORIGINS=

# Optional: override the book catalog CSV path.
# Defaults to the bundled api/data/books.csv.
DATA_PATH=
