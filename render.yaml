services:
  - type: web
    name: tome-api
    runtime: docker
    rootDir: api
    plan: free
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
